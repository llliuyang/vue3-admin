<template>
  <div class="sidebar-logo-container" :class="{ collapse: collapse }">
    <transition name="sidebarLogoFade">
      <!--      sidebar收起状态下：有logo显示logo，没logo显示标题-->
      <router-link
        v-if="collapse"
        to="/"
        key="collapse"
        class="sidebar-logo-link"
      >
        <img
          v-if="logo"
          :src="logo"
          class="sidebar-logo"
          alt="VueElementAdmin"
        />
        <h2 v-else class="sidebar-title">{{ title }}</h2>
      </router-link>
      <!--      sidebar展开状态下：有logo显示logo，标题一直展示-->
      <router-link v-else to="/" class="sidebar-logo-link" key="expand">
        <img
          v-if="logo"
          :src="logo"
          class="sidebar-logo"
          alt="VueElementAdmin"
        />
        <h2 class="sidebar-title">{{ title }}</h2>
      </router-link>
    </transition>
  </div>
</template>

<script lang="ts" setup>
import logo from "@/assets/vue.svg"

defineProps({
  collapse: {
    type: Boolean,
    required: true
  }
})

const title = "Vue Element Admin"
</script>

<style lang="scss" scoped>
.sidebar-logo-container {
  width: 100%;
  height: 50px;
  line-height: 50px;
  text-align: center;
  background: #2b2f3a;
  overflow: hidden;
  .sidebar-logo-link {
    display: block;
    width: 100%;
    height: 100%;
    .sidebar-logo {
      width: 32px;
      height: 32px;
      vertical-align: middle;
      margin-right: 12px;
    }
    .sidebar-title {
      display: inline-block;
      color: #fff;
      margin: 0;
      font-weight: 600;
      line-height: 50px;
      font-size: 14px;
      font-family: Avenir, Helvetica Neue, Arial, Helvetica, sans-serif;
      vertical-align: middle;
    }
  }
  &.collapse {
    .sidebar-logo {
      margin-right: 0;
    }
  }
}
.sidebarLogoFade-enter-active {
  transition: opacity 1.5s;
}
.sidebarLogoFade-enter-from,
.sidebarLogoFade-leave-to {
  opacity: 0;
}
</style>
